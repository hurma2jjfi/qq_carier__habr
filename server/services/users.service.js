// Здесь будет бизнес-логика, например, работа с БД
// Пока заглушка

exports.findAllUsers = () => {
    return [{ id: 1, name: 'Иван' }];
  };
  
  exports.createUser = (userData) => {
    // Сохраняем пользователя (заглушка)
    return { id: 2, ...userData };
  };
  